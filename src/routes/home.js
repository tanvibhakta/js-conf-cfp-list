import styled from "styled-components";
import List from "../components/List";

const HomeContainer = styled.div`
`

const Home = () => {
	return (
		<HomeContainer>
			<List />
		</HomeContainer>
	);
}

export default Home;
